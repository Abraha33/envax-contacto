# ENVAX — Mi selección (deprecated concept)

`Mi selección` is no longer the canonical persistence model.

It was replaced by **multiple named favorites lists**.

Read: `docs/FAVORITES.md`.

Canonical examples:
- Halloween
- Cumpleaños
- Uso diario

Do not reintroduce a single cart-like selection unless explicitly approved later.
