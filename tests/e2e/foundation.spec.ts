import { test } from '@playwright/test';

test.skip('customer shell smoke is enabled once browser E2E enters the implementation gate', async () => {
  // Foundation only establishes the Playwright harness.
});
